import { useState } from 'react';import { useNavigate,useParams } from 'react-router-dom';import ApplicationForm from '../components/applications/ApplicationForm';import { Spinner } from '../components/common/UI';import { useAuth } from '../hooks/useAuth';import { useApplications } from '../hooks/useApplications';import { createApplication,updateApplication } from '../services/applicationService'
export default function ApplicationEditorPage(){const {id}=useParams();const nav=useNavigate();const {user}=useAuth();const {data,loading}=useApplications();const [saving,setSaving]=useState(false);const current=data.find(a=>a.id===id);if(id&&loading)return <Spinner/>;const save=async(form)=>{setSaving(true);try{if(id)await updateApplication(user.uid,id,form,current.currentStatus);else await createApplication(user.uid,form);nav(id?`/applications/${id}`:'/applications')}finally{setSaving(false)}};return <div className="page"><header className="page-head"><div><span className="eyebrow">{id?'Keep it current':'A new opportunity'}</span><h1>{id?'Edit application':'Add application'}</h1><p>Capture the details you need to make your next move.</p></div></header><ApplicationForm initial={current} applications={data} onSubmit={save} saving={saving} onCancel={()=>nav(-1)}/></div>}
